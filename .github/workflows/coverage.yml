name: Coverage
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: npm ci
      - run: npm run build --if-present
      - run: npm test
      - run: npm run coverage
